#!/usr/bin/env bash

dir="$HOME/.config/bspwm/rofi/themes"
rofi_command="rofi -theme $dir/links.rasi"

# Browser
app="chromium --incognito"

# Links
arch=""
github=""
search=""
mail=""
youtube=""

# Variable passed to rofi
options="$arch\n$github\n$search\n$mail\n$youtube"

chosen="$(echo -e "$options" | $rofi_command -p "Quicklinks | Chromium Incognito" -dmenu -selected-row 2)"
case $chosen in
    $arch)
        $app 'https://www.archlinux.org' &
        ;;
    $github)
        $app 'https://www.github.com' &
        ;;
    $search)
        $HOME/.config/bspwm/rofi/bin/duckgoo
        ;;
    $mail)
        $app 'https://www.gmail.com' &
        ;;
    $youtube)
        $HOME/.config/bspwm/rofi/bin/yt
        ;;
esac
